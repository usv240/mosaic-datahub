line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
# Long SQL strings and user-facing evidence text are clearer intact; Ruff formatter
# handles code layout. Nested async context managers mirror the MCP client lifecycle.
ignore = ["E501", "SIM117"]
