# Integration evidence â€” 2026-07-29

All live checks used uniquely named synthetic assets against local DataHub Core.

| Surface | Verified behavior | Result |
|---|---|---|
| DataHub SDK | Schema, fine-grained lineage, downstream blast radius | Pass |
| Official MCP server 3.4.4 | Search, column lineage, field-tag mutation and reread | Pass |
| DuckDB | Exact allowlisted aggregate; zero raw rows returned | Pass |
| Governed write-back | Tag, `mosaic.riskState`, threat Document, active incident reread | Pass |
| Agent Context Kit | Existing dataset-level directional path | Pass manually |
| Agent Context Kit fresh fine-grained edge | Bounded probe on this version | Not reliable; not claimed |

The complete approved live workflow returned `passed` and produced an active
`PRIVACY_RISK` incident. MCP exposed `get_lineage_paths_between` as well as search,
entity, lineage, tag, property, and Document tools.

The Agent Context Kit caveat is deliberately retained: SDK and MCP column-level paths
are the production evidence surfaces for this submission until fresh-edge ACK behavior
is stable in the pinned local versions.
