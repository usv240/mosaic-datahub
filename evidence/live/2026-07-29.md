# Live integration proof â€” 2026-07-29

Fresh execution against a local DataHub Core quickstart. All assets created by the
probe used a unique `mosaic.*_<timestamp>` synthetic name; no production catalog or
person-level data was used.

## Result

`uv run mosaic live-demo --approve-writeback` returned `status: passed`.

| Check | Result |
|---|---|
| DataHub schema re-read | Pass |
| Fine-grained column lineage re-read | Pass |
| DuckDB count-only aggregate execution | Pass |
| Raw person-level rows returned | 0 |
| Approved field-tag re-read | Pass |
| Linked threat-model Document re-read | Pass |

The executed aggregate was:

```sql
SELECT zip5, birth_date, gender_category, COUNT(*) AS equivalence_class_size
FROM research_export_clean
GROUP BY zip5, birth_date, gender_category
```

The frozen fixture returned 120 total records, 120 distinct combinations, `minimum_k`
of 1, and 100% of records below `k=5`. The tag was
`mosaic:validated-critical`; the Document subtype was `Privacy Threat Model`.

## Boundary

A later complete run additionally verified the `mosaic.riskState` structured property,
an active `PRIVACY_RISK` incident, and official MCP search/column-lineage/tag reread.
Fresh-edge Agent Context Kit behavior remains version-sensitive and is not claimed.
