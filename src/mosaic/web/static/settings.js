(function(){"use strict";const id=(value)=>document.getElementById(value);let csrf="";function status(message){id("publish-status").textContent=message}fetch("/api/health/datahub").then((response)=>response.json()).then((data)=>{id("server").textContent=data.server;id("connection").textContent=data.status.replaceAll("_"," ");if(!data.web_writeback_enabled){status(data.public_demo?"Read-only hosted demo: publication is disabled.":"Set MOSAIC_ENABLE_WEB_WRITEBACK=true locally to unlock approval.");return}return fetch("/api/approval-token").then((response)=>response.json()).then((token)=>{csrf=token.csrf_token;id("publish").disabled=false;status("Approval gate ready. Exact confirmation is still required.")})}).catch(()=>status("Could not load operator status."));id("probe").addEventListener("click",()=>{id("connection").textContent="Testing…";fetch("/api/health/datahub?probe=true").then((response)=>response.json()).then((data)=>id("connection").textContent=data.status).catch(()=>id("connection").textContent="unavailable")});id("publish").addEventListener("click",()=>{id("publish").disabled=true;status("Publishing and re-reading DataHub evidence…");fetch("/api/publish",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({csrf_token:csrf,confirmation:id("confirmation").value})}).then(async(response)=>{const data=await response.json();if(!response.ok)throw new Error(data.detail||"Publication failed");status(data.status==="published"?"Published. Every evidence type was re-read successfully.":`Result: ${data.status}`)}).catch((error)=>{status(error.message);id("publish").disabled=false})})})();
