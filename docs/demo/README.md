# Submission video

`mosaic-submission-demo.mp4` is the final narrated submission artifact. It combines nine verified product captures with the interactive browser walkthrough.

- Duration: 160.625 seconds
- Video: H.264, 1280x720, 24 fps
- Audio: AAC, mono
- Size: 7,156,977 bytes
- SHA-256: `b86004014facfae578f1ebffe1d5d0ecd56256507e5e706f059d7247b122869a`

`narration.txt` is the exact voice-over. `08-product-walkthrough.webm` remains the interactive source footage, and `media-manifest.json` contains the CI-produced hashes for the source walkthrough and ten screenshots.

The MP4 is upload-ready. The hackathon still requires a public YouTube or Vimeo URL; that external receipt must not be marked complete until an authenticated upload succeeds.
